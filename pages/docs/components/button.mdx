import { serverSideTranslations } from 'next-i18next/serverSideTranslations';

import { NestedLayout } from '@/components/layout/NestedLayout';
import ComponentExample from '@/components/layout/ComponentExample';
import { Code } from '@/components/layout/Code';
import { PrevAndNext } from '@/components/layout/PrevAndNext';
import { SidebarLink } from '@/components/layout/SidebarLink';

import { Default } from '@/components/docs/components/button/Default';
import { Border } from '@/components/docs/components/button/Border';
import { Transparent } from '@/components/docs/components/button/Transparent';

export const meta = {
  name: 'Button',
  title: 'Button | Lazydev ui components',
};

export const getStaticProps = async ({ locale }) => {
  return {
    props: {
      ...(await serverSideTranslations(locale, ['home'])),
    },
  };
};

<SidebarLink>
  <a href="#default" className="sidebar__link">
    Default
  </a>
</SidebarLink>

<PrevAndNext leftPath="/docs/theme" rightPath="/docs/components/alert" />

<div id="default" />

<ComponentExample name="Default" paragraph="This is a paragraph">
  <Default />
</ComponentExample>

<Code>
```jsx
import React, { useState } from 'react';

export const Default = () => {
  const [active, setActive] = useState(true);

  return (
    <div className="flex items-center space-x-3">
      <a
        onClick={() => setActive(true)}
        className={`${
          active ? '-translate-y-1' : ''
        } w-fit mx-auto rounded-lg py-2 px-8 font-medium shadow-md text-sm duration-300
          active:bg-opacity-80 cursor-pointer ease-in-out active:scale-95 bg-[#1a5cff] 
          dark:bg-[#1a5cff] md:text-sm text-white hover:shadow-md`}>
        Active
      </a>
      <a
        onClick={() => setActive(false)}
        className={`${
          active ? '' : '-translate-y-1'
        } w-fit mx-auto rounded-lg py-2 px-8 font-medium shadow-md text-sm duration-300
          active:bg-opacity-80 cursor-pointer ease-in-out active:scale-95 bg-[#1a5cff] 
          dark:bg-[#1a5cff] md:text-sm text-white hover:shadow-md`}>
        Default
      </a>
    </div>
  );
};


````
</Code>

<div id="border" />

<ComponentExample name="Border" paragraph="This is a paragraph">
  <Border />
</ComponentExample>

<Code>
```jsx
import React, { useState } from 'react';

export const Border = () => {
  const [active, setActive] = useState(true);

  return (
    <div className="flex items-center space-x-3">
      <a
        onClick={() => setActive(true)}
        className={`${
          active ? 'bg-[#1a5cff] text-white' : ''
        } w-fit mx-auto rounded-lg py-2 px-8 cursor-pointer active:scale-95
        shadow-md text-sm duration-300 border border-[#1a5cff] active:bg-opacity-80
        ease-in-out bg-transparent md:text-sm text-[#1a5cff]`}>
        Active
      </a>
      <a
        onClick={() => setActive(false)}
        className={`${
          active ? '' : 'bg-[#1a5cff] text-white'
        } w-fit mx-auto rounded-lg py-2 px-8 cursor-pointer active:scale-95
        shadow-md text-sm duration-300 border border-[#1a5cff] active:bg-opacity-80
        ease-in-out bg-transparent md:text-sm text-[#1a5cff]`}>
        Default
      </a>
    </div>
  );
};
````

</Code>


<div id="transparent" />

<ComponentExample name="Transparent" paragraph="This is a paragraph">
  <Transparent />
</ComponentExample>

<Code>
```jsx
import React, { useState } from 'react';

export const Transparent = () => {
  const [active, setActive] = useState(true);

  return (
    <div className="flex items-center space-x-3">
      <a
        onClick={() => setActive(true)}
        className={`${
          active ? 'bg-blue-500 bg-opacity-30' : ''
        } w-fit mx-auto rounded-lg py-2 px-8 cursor-pointer active:scale-95
          shadow-md text-sm duration-300 ease-in-out hover:bg-blue-500 hover:bg-opacity-30
          md:text-sm text-[#1a5cff]`}>
        Active
      </a>
      <a
        onClick={() => setActive(false)}
        className={`${
          active ? '' : 'bg-blue-500 bg-opacity-30'
        } w-fit mx-auto rounded-lg py-2 px-8 cursor-pointer active:scale-95
          shadow-md text-sm duration-300 ease-in-out hover:bg-blue-500 hover:bg-opacity-30
          md:text-sm text-[#1a5cff]`}>
        Default
      </a>
    </div>
  );
};
````

</Code>

export default ({ children }) => (
  <NestedLayout name={meta.name} title={meta.title}>
    {children}
  </NestedLayout>
);
