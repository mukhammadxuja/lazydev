
import { NestedLayout } from '@/components/layout/NestedLayout';
import { PrevAndNext } from '@/components/layout/PrevAndNext';

export const meta = {
  name: 'Tooltip',
  title: 'Tooltip | Lazydev ui components',
};

<PrevAndNext leftPath="/docs/components/avatar" rightPath="/docs/components/dialog" />

<div className="h-screen"></div>

export default ({ children }) => (
  <NestedLayout name={meta.name} title={meta.title}>
    {children}
  </NestedLayout>
);

