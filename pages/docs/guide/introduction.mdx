import { serverSideTranslations } from 'next-i18next/serverSideTranslations';

import { NestedLayout } from '@/components/layout/NestedLayout';
import { PrevAndNext } from '@/components/layout/PrevAndNext';

import { Introduction } from '@/components/docs/components/guide/Introduction'

export const meta = {
  name: 'Introduction',
  title: 'Introduction | Lazydev ui components',
};

export const getStaticProps = async ({ locale }) => {
  return {
    props: {
      ...(await serverSideTranslations(locale, ['home'])),
    },
  };
};

<PrevAndNext leftPath="#!" rightPath="/docs/theme" />


<Introduction />

export default ({ children }) => (
  <NestedLayout name={meta.name} title={meta.title}>
    {children}
  </NestedLayout>
);

